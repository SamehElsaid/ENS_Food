import React from 'react'

function BtnHome({children}) {
  return (
    
  <div className="border || w-fit || select-none || border-mainColor || text-sm || text-mainColor || rounded-full || hover:bg-mainColor || hover:text-white || duration-500">
    {children}
  </div>
  )
}

export default BtnHome